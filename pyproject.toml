[project]
name = "ignsniper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AniDevWired", email = "ethical.999.aniruddha@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
ignsniper = "ignsniper:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
